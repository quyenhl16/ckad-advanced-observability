apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-ingest.fullname" . }}
  labels:
    {{- include "traffic-ingest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "traffic-ingest.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
