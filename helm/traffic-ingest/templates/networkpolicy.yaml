{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "traffic-ingest.fullname" . }}
  labels:
    {{- include "traffic-ingest.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "traffic-ingest.selectorLabels" . | nindent 6 }}
  policyTypes: [Ingress, Egress]
  ingress:
    - ports:
        - {protocol: TCP, port: {{ .Values.service.port }}}
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.analyticsPodSelector | nindent 14 }}
      ports:
        - {protocol: TCP, port: 8081}
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - {protocol: UDP, port: 53}
        - {protocol: TCP, port: 53}
{{- end }}
