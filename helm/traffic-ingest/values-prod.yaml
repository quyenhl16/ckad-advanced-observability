image:
  repository: registry.example.com/ckad/traffic-ingest
  tag: "1.0.0"
  pullPolicy: IfNotPresent

config:
  analyticsURL: http://analytics-engine.advanced-observability.svc.cluster.local:8081
  httpClientTimeout: 3s

autoscaling:
  minReplicas: 3
  maxReplicas: 8

canary:
  image:
    repository: registry.example.com/ckad/traffic-ingest-canary
    tag: "1.1.0"
