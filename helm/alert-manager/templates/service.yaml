apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-manager.fullname" . }}
  labels:
    {{- include "alert-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "alert-manager.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
