apiVersion: v1
kind: Service
metadata:
  name: {{ include "observer-demo.fullname" . }}
  labels:
    {{- include "observer-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "observer-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
