apiVersion: v1
kind: Service
metadata:
  name: traffic-ingest-bg
  namespace: ckad-labs
spec:
  selector: {app: traffic-ingest-bg, version: blue}
  ports:
    - name: http
      port: 8080
      targetPort: http
