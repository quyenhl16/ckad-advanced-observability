apiVersion: apps/v1
kind: Deployment
metadata:
  name: day4-backend
  namespace: ckad-labs
  labels: {app: day4-backend, lab: "4.1"}
spec:
  replicas: 2
  selector:
    matchLabels: {app: day4-backend}
  template:
    metadata:
      labels: {app: day4-backend, tier: backend, lab: "4.1"}
    spec:
      automountServiceAccountToken: false
      securityContext: {runAsNonRoot: true, runAsUser: 65532, runAsGroup: 65532}
      containers:
        - name: backend
          image: ckad/traffic-ingest:local
          imagePullPolicy: IfNotPresent
          env:
            - {name: HTTP_ADDR, value: ":8080"}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe:
            httpGet: {path: /health/ready, port: http}
            periodSeconds: 3
          resources:
            requests: {cpu: 25m, memory: 24Mi}
            limits: {cpu: 200m, memory: 96Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: day4-frontend
  namespace: ckad-labs
  labels: {app: day4-frontend, lab: "4.1"}
spec:
  replicas: 2
  selector:
    matchLabels: {app: day4-frontend}
  template:
    metadata:
      labels: {app: day4-frontend, tier: frontend, lab: "4.1"}
    spec:
      automountServiceAccountToken: false
      securityContext: {runAsNonRoot: true, runAsUser: 65532, runAsGroup: 65532}
      containers:
        - name: frontend
          image: ckad/observability-frontend:local
          imagePullPolicy: IfNotPresent
          env:
            - {name: HTTP_ADDR, value: ":8080"}
            - {name: ANALYTICS_URL, value: "http://analytics-engine.advanced-observability.svc.cluster.local:8081"}
            - {name: ALERT_MANAGER_URL, value: "http://alert-manager.advanced-observability.svc.cluster.local:8082"}
          ports: [{name: http, containerPort: 8080}]
          readinessProbe:
            httpGet: {path: /health/ready, port: http}
            periodSeconds: 3
          resources:
            requests: {cpu: 25m, memory: 24Mi}
            limits: {cpu: 150m, memory: 96Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
